<?php

/* 
 * You need a defined set of signals/events your app generates and a way for plugins 
 * to register their interest in acting on them (actions) or modifying the default action (filters). 
 * Then, you document that API so people can design their code around it. 
 * 
 * Read about the publish-subscribe pattern. 
 * 
 * WordPress has a set of actions and filters that it invokes as it processes a page, 
 * and plugins can register their interest in receiving notifications about these events.
 * 
 */

